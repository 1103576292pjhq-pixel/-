# Synthesis Environment Check (2026-05-06)

This file records the local environment status for the RTL handoff package. It is not a synthesis report and does not contain real 28nm area, power, timing, WNS, TNS, or frequency numbers.

## Tool Availability

| Tool | Status | Observed path or blocker | Use in this package |
| --- | --- | --- | --- |
| `iverilog` | available | `D:\iverilog\bin\iverilog.exe` | RTL functional regression. |
| `vvp` | available | `D:\iverilog\bin\vvp.exe` | RTL simulation execution. |
| `python` | available | `D:\Program Files\Python312\python.exe` | MX reference model, vector generation, precision stats, waveform PNG rendering. |
| `gtkwave` | available | `D:\iverilog\gtkwave\bin\gtkwave.exe` | Optional VCD viewing; PNG evidence is generated by Python. |
| `yosys` | not found | `BLOCKED_NO_SYNTH_TOOL` | Generic structural netlist/stat cannot be generated locally. |
| `openroad` | not found | `BLOCKED_NO_SYNTH_TOOL` | Open-source backend flow not available locally. |
| `verilator` | not found | `BLOCKED_NO_SYNTH_TOOL` | Lint/cycle simulation not available locally. |
| `dc_shell` | not found | `BLOCKED_NO_SYNTH_TOOL` | Synopsys Design Compiler flow not available locally. |
| `genus` | not found | `BLOCKED_NO_SYNTH_TOOL` | Cadence Genus flow not available locally. |
| `innovus` | not found | `BLOCKED_NO_SYNTH_TOOL` | Physical implementation flow not available locally. |

Command used by the acceptance script:

```powershell
Get-Command iverilog,vvp,python,gtkwave,yosys,openroad,verilator,dc_shell,genus,innovus -ErrorAction SilentlyContinue
```

## 28nm Library Availability

Workspace search found no real 28nm `.db` or `.lib` files outside ignored/internal output directories.

Status:

```text
BLOCKED_NO_28NM_LIB
```

This means the repository can provide an RTL handoff package, SDC template, and synthesis script templates, but it cannot produce a real 28nm mapped gate-level netlist or real PPA reports in this environment.

## Current Synthesis Deliverables

| File | Status | Meaning |
| --- | --- | --- |
| `constraints/mx_array_32x16.sdc` | present | Starting-point clock and I/O delay constraints; backend must tune with real corner and target period. |
| `synth/run_dc_template.tcl` | present | Design Compiler template; requires real `TARGET_LIBRARY` and `LINK_LIBRARY`. |
| `synth/run_yosys_generic.ys` | present | Generic structural sanity script; cannot run locally until `yosys` is available. |
| `docs/report/07_synthesis_and_ppa.md` | present | Report boundary and backend method explanation. |
| `docs/report/12_backend_handoff_checklist.md` | present | Backend receiving checklist. |

## Verdict For This Handoff

The correct acceptance status is:

```text
PASS_WITH_EXTERNAL_SYNTH_BLOCKER
```

This status is valid only if functional regression, Python reference checks, waveform evidence, evidence index checks, pure Verilog checks, and package exclusion checks pass. The blocker is external synthesis/PPA infrastructure, not RTL functional evidence.
